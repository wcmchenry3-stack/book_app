[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    security: OWASP-aligned security tests (injection payloads, auth bypass, upload validation)
    benchmark: performance benchmarks (requires pytest-benchmark; skipped in normal CI)
