name: Sync main → dev

on:
  push:
    branches: [main]

jobs:
  sync:
    uses: wcmchenry3-stack/.github/.github/workflows/called-sync-main-to-dev.yml@a973bcce934fef46e22d40123430ad2594d5bada
    secrets: inherit
