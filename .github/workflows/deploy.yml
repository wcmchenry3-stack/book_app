name: Deploy

on:
  push:
    branches: [main]

jobs:
  ci:
    uses: wcmchenry3-stack/book_app/.github/workflows/ci.yml@main
    secrets: inherit

  deploy:
    needs: ci
    uses: wcmchenry3-stack/.github/.github/workflows/called-deploy-render.yml@main
    with:
      service-name: 'bookshelf-api'
      service-id: 'srv-d6vkel7fte5s73du7fc0'
      zap-target-url: 'https://bookshelf-api-rxp3.onrender.com'
    secrets: inherit
